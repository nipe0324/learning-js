import Card from './Card';
import { center } from '../styled-system/patterns';

function App() {
  return (
    <div className={center({ height: "100vh" })}>
      <Card />
    </div>
  );
}

export default App;
